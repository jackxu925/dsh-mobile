# DSH Mobile UX 审计清单

> 2026-08-31 · 针对 `web/`（app.js / style.css / index.html）的全面走查。
> 基线：Vercel Web Interface Guidelines + iOS 移动体验惯例 + 真实使用反馈。
> 状态列：✅ 已修复 / ⏳ 待修 / 📝 记录为已知限制。

## P0 — 功能性痛点（最直接影响"难用感"）

| # | 问题 | 修法 | 状态 |
|---|------|------|------|
| 1 | 输入框字号 15.5px < 16px，iOS 每次聚焦自动放大页面再缩回 | 所有输入控件 ≥16px | ✅ |
| 2 | 点发送后要等服务器往返才出气泡，感觉卡顿 | 乐观上屏（半透明气泡），用 source.rpcId 精确去重；失败可点重试 | ✅ |
| 3 | 「待办」tab 是假的（点了只回列表） | 真过滤：只显示有待审批/待回答的会话 + 空态 | ✅ |
| 4 | 运行中发的消息进入队列但界面完全不可见 | 处理 `session/queue` 帧，显示"已排队"气泡 | ✅ |
| 5 | 新建会话没有 Agent 预设选择 | `agentPreset.list` → 预设 chips（标准/PTC/极简） | ✅ |
| 6 | 不能发图片（手机上"截图给 AI 看"是高频场景） | composer 加图片按钮 → 压缩 → image part（遵循 imageLimits）；会话内图片块可显示 | ✅ |
| 7 | 审批/提问的 toast 提示不可点 | 点击 toast 直达对应会话 | ✅ |
| 8 | 历史加载失败只有文字、无重试 | 失败态可点击重试 | ✅ |
| 9 | 断线时聊天页无感知，还能"发送" | 副标题显示连接状态；断线禁用发送 | ✅ |

## P1 — 交互细节

| # | 问题 | 修法 | 状态 |
|---|------|------|------|
| 10 | 列表无手动刷新手段 | 下拉刷新 | ✅ |
| 11 | 聊天气泡没有时间戳 | 用户/助手气泡下挂 11px  muted 时间 | ✅ |
| 12 | 「已结束」徽标语义误导 | 改为「空闲」 | ✅ |
| 13 | 冷启动白屏（JS 加载前一片白） | 内联深色 splash | ✅ |
| 14 | 搜索框会触发自动大写/拼写检查 | autocapitalize=off、spellcheck=false | ✅ |
| 15 | 发送按钮无反馈 | 点击即振动 + 乐观气泡已覆盖等待感 | ✅ |

## P2 — 规范与打磨

| # | 问题 | 修法 | 状态 |
|---|------|------|------|
| 16 | toast 无 aria-live；icon-only 按钮无 aria-label | 补齐 | ✅ |
| 17 | `transition: all`（toast） | 明确列出属性 | ✅ |
| 18 | 动画未降级 | prefers-reduced-motion 全局降级 | ✅ |
| 19 | 缺 touch-action: manipulation（双击缩放延迟） | 交互元素统一加 | ✅ |
| 20 | markdown 表格无渲染，竖线文本直接糊开 | 表格块降级为等宽横滚 pre | ✅ |
| 21 | iPad/横屏内容全宽铺开难读 | 内容列 max-width 760px 居中 | ✅ |
| 22 | `user-scalable=no` 被指南标记 | 有意保留（App 感），已注释说明；配合 #1 修复后不再误触发缩放 | ✅ 记录 |
| 23 | 超长会话全量 DOM 重建 | 已有 80ms 节流 + 分页加载；虚拟化列为已知限制 | 📝 |
| 24 | 思考过程只显示一行提示 | 已知限制：桌面端可见完整推理 | 📝 |

---

# 第二轮（2026-09-19 · 按 11 条用户流程全覆盖走查）

> 第一轮 24 项复核无回退。本轮按「启动 → 连接 → 列表 → 打开会话 → 发消息 → 审批 → 提问 → 停止 → 新会话 → 会话设置 → 手势」逐流程复查。

## P0 — 缺陷与「承诺落空」

| # | 问题 | 修法 | 状态 |
|---|------|------|------|
| 25 | 「加载更早的消息」后阅读位置丢失（DOM 重建后视口跳到更旧内容） | loadEarlier 记录 `scrollHeight - scrollTop` 锚点，渲染后按差值恢复 | ✅ |
| 26 | 会话被其它端删除时，正在看的聊天页变僵尸页（还能输入、发送必败） | `host/session-removed` 命中当前会话 → toast 提示并退回列表 | ✅ |
| 27 | iOS「添加到主屏幕」图标失效（apple-touch-icon 指向 SVG，iOS 不支持） | 由 icon.svg 栅格化 icon-180.png / icon-512.png；index.html、manifest、routes 同步更新 | ✅ |
| 28 | 本机新建的空会话从列表消失（blank 被过滤），用户以为丢了 | 会话对象加 `createdHere`，本机创建的空会话保持可见 | ✅ |
| 29 | 列表空态文案 `\n` 不换行 | `.empty-state` 加 `white-space: pre-line` | ✅ |
| 30 | 历史图片加载失败静默移除，消息「少一块」用户无感知 | 失败替换为「图片加载失败 · 点按重试」占位按钮 | ✅ |

## P1 — 高频流程缺口

| # | 问题 | 修法 | 状态 |
|---|------|------|------|
| 31 | 翻历史时来新消息毫无感知（nearBottom 门控停滚动是对的，但无提示） | 「↓ 新消息」悬浮 pill：不打断阅读，点按回底部；滚到底自动隐藏 | ✅ |
| 32 | 代码块手机端难复制（只能长按全选） | 代码块右上角「复制」按钮（含非安全上下文 execCommand 降级） | ✅ |
| 33 | 消息图片不能放大，截图小字看不清 | 点击消息图片打开全屏查看器，点任意处关闭 | ✅ |
| 34 | 输入一半切走/被杀后台，草稿全丢 | 按会话 id 存 localStorage，进入会话自动恢复，发送后清除 | ✅ |
| 35 | 提问提交后看不到自己答了什么 | 卡片保留答案摘要（选项 + 自定义回答） | ✅ |
| 36 | 审批命令截断 600 字符后无法看全 | 「展开全文」按钮 | ✅ |
| 37 | 危险命令（rm -rf / sudo / push --force 等）与普通命令同一确认强度 | 命中危险模式 → 「允许」需 3 秒内再点一次确认，按钮变红 | ✅ |
| 38 | 断线只能等 15s 轮询重连 | conn-pill 可点按手动重连，文案提示「已断开 · 点按重连」 | ✅ |
| 39 | 停止按钮 32px < 44px 触控标准，且是用户着急时最想按的按钮 | min-height 44px、加大内边距 | ✅ |
| 40 | 不能直接粘贴剪贴板截图 | paste 优先处理 clipboardData.files 中的图片 | ✅ |
| 41 | 会话设置面板无标题；模型加载失败无重试 | sheet 顶部加会话标题；失败附「重试」按钮 | ✅ |

## P2 — 打磨与规范

| # | 问题 | 修法 | 状态 |
|---|------|------|------|
| 42 | `--text-3` (#6b7382) 在深色背景上约 4.0:1，小字（时间戳/副标题/tab）对比度不足 | 提升为 #7d8590（≥4.6:1） | ✅ |
| 43 | 时间格式无「昨天」、跨三年不显年份 | fmtTime 补充分层 | ✅ |
| 44 | 跨天会话无日期分隔，难以定位 | 渲染时按天插入日期分隔条 | ✅ |
| 45 | 会话卡/工具卡头为 div + onclick，无键盘与读屏语义 | role="button"、tabindex、Enter/Space 触发；工具卡补 aria-expanded；全局 :focus-visible 焦点环 | ✅ |
| 46 | 切换会话时输入框残留上一个会话的文字 | 草稿恢复逻辑顺带覆盖：进入会话即恢复该会话草稿或清空 | ✅ |
| 47 | 刚活跃的会话埋在工作区分组中部，组间顺序固定不变 | 分组按「组内最近活跃」排序；列表顶部加「继续上次会话」快捷卡（localStorage 记录最近打开） | ✅ |
| 48 | 多工作区并行使用时，只能看到一个「最近会话」，无法同时纵览所有进行中的对话 | 首页加视图切换分段控件：「最近活跃」（全部会话平铺、按最后活跃时间降序、卡片标注所属工作区）/「按工作区」（原分组视图）；选择持久化（`dshm-list-mode`），默认「最近活跃」；时间视图下不显示续聊卡（第一张卡即最近会话）；待办页隐藏切换控件 | ✅ |
| 49 | agent 自排的任务清单（`todo_write`）当普通消息塞在对话时间线里，往上滚两屏就丢失；而它恰恰是需要随时瞟一眼的东西 | 改为顶部悬置任务条：读宿主 `todos` 投影（`turn/start` 清空、`todo/write` 全量覆盖），会话页导航栏下方常驻一行「当前步骤 + n/N + 2px 进度线」，点开底部抽屉看全量（进行中置顶）；全部完成后整条变绿「N 项全部完成」并在 3 秒后收成一条绿细线（点细线仍可打开抽屉，会话 ⋯ 菜单也有「任务清单」入口）；时间线不再出现待办卡，`todo_write` 的调用/结果卡同样隐去 | ✅ |
| 50 | 增量投影帧是「单键单值」`{sessionId,key,value}`，而客户端只解析 `values` 整块，导致 title/permissions/todos 等实时更新全部失效（只有重开流或刷新列表才会变） | `Mux.handlers.control` / `handlers.follow` 统一走 `applyProjectionFrame()`：`key` 形式包成 `{[key]:value}` 再落地 | ✅ |
| 51 | 宿主的 `tool/result` 消息只有 `{source,content,role,id}`，**不带 callId**，客户端靠"最近一个未完成工具卡"兜底配对，并发调用会串卡、被跳过的卡则留下空的 `tool {}` 幽灵卡 | 工具调用入队 `_pendingCalls`，结果无 id 时按调用顺序出队配对（带 id 的从队列摘除）；兜底逻辑保留 | ✅ |
| 52 | 思考型消息（只有 reasoning、没有正文）单独成条 → 对话流里一排没有任何文字的泡泡（实测一个会话 381 条 assistant 消息里 100 条属于此列） | 这类消息不再入时间线：思考攒在 `_thinkBuf` 上，挂到下一条内容（工具卡头部的思考图标 / 后续气泡的 meta 图标）；本轮结束仍无承载者时落成一行「思考过程」入口；分页加载（loadEarlier）改用带全字段的临时会话对象折叠，并把页尾悬空的思考补给下一页首条 | ✅ |
| 53 | 列表「空闲」标签看着没居中 | 该状态没有状态点，但空 `b-dot` 仍占 6px + 5px 间距，把文字顶偏 ~6px：`.badge.done .b-dot { display:none }` | ✅ |
| 54 | 新会话 FAB 的「+」不居中（重心比按钮中心高 ~3px） | 图标槽 `.ic-slot` 改 inline-flex 居中、`svg` 改 block，消掉 inline 基线对齐带来的偏移 | ✅ |
| 55 | 横屏再转回竖屏后对话字体被放大，要退出会话重进才恢复 | WebKit 文本自动放大（text autosizing）按新布局宽度重算字号且逐元素粘滞，重建 DOM 才会消失；`html { -webkit-text-size-adjust:100% }` 从根上禁用该机制；顺带把仍为 15px 的排队编辑框提到 16px（<16px 的可编辑框会触发 iOS 聚焦自动缩放） | ✅ |
| 56 | 键盘弹起时点发送：第一次点只收键盘不发送，点第二次才发 | 时序＝touchend → iOS 收键盘 → `#app` 高度复原 → 按钮位移 → 浏览器把随后的合成 click 判为点到别处而丢弃。发送改为在 touchend 就执行（带 10px 移动熔断、保留 420ms 长按反向）；`＋`图片、排队 chip、「↓新消息」pill 同样改走 onTap；鼠标/键盘路径仍走 click（500ms 防重入）。A/B 实测：旧代码第一次点 0 条消息、第二次 1 条；新代码第一次点即 1 条且不重复 | ✅ |
| 57 | 「按工作区」视图把每个工作区里的会话全部铺开，多工作区并行时首屏全是别人的对话，找目标工作区要滚很久 | 改成两级：第一级只显示工作区行（图标 + 名称 + 最新会话名 · 会话数 · 最近活跃时间），按组内最近活跃降序；点按下钻才看该工作区的会话卡（同样按最近活跃降序），顶部「‹ 全部工作区」返回，大标题同步显示工作区名；进会话再返回保留钻取态；搜索（跨工作区检索）改为平铺结果 + 卡片标注工作区，比钻取更直接 | ✅ |
| 58 | 列表顶部「继续上次会话」入口已无必要：两级工作区视图与「最近活跃」时间视图都能一步直达最近对话 | 删除置顶续聊卡（渲染调用、resumeRow 函数与全部样式） | ✅ |
| 59 | 运行中排队一条消息：它混进对话流里显示，输入框上方的排队 chip 反而不出现；退出重进才变成正确形态（chip 在、对话流没有） | 实时路径与快照路径对齐：`session/prompt` 受理后，若最终 mode=queue 且本轮仍在跑，就撤下乐观气泡并立即重渲染队列条——排队 chip 即刻出现；本轮结束消息被消费时经 user/message 事件正常落入对话流，chip 同步消失。空闲新开一轮/插话路径不变（气泡保留、rpcId 转正） | ✅ |
| 60 | 对话流里的裸 URL 被污染：`**http://…**` 加粗链接点开是 `…sheet.html%3C/strong%3E` | 修 md() 管线两处：URL 字符类排除 `*`（收尾 `**` 不再被吞进 URL，粗体替换也就不会把 `<strong>` 注进 href）；同时排除全部非 ASCII（`\u0080-\uffff`），URL 后紧跟的中文/全角标点不再被吞（如「http://x.com/a）还有」）；句尾 ASCII 标点（`.` `,` `;` `!` `?`）移出链接外。单测 9 例全过（加粗链接/中文句号/全角括号/md 链接/查询串/行内代码链接化），并在真实会话里对原报告消息验证 href 干净 | ✅ |
| 61 | ⋯ 菜单实测 1958px ≈ 3.4 屏：15 个模型摊开各带描述 + 权限 + 统计 + 发送设置，改个模型要先滑过一整段 | 方案 A（原型 sheet.html 选定）：菜单收成 5 行设置行，行右侧常驻当前值（「glm-5.3 · Max ›」「完全访问 ›」「36% · 362k」），高度 430px < 1 屏；点行进入从右滑入的二级推送面板（‹ 返回）：模型面板顶部平铺当前模型的思考强度 chips（只换强度 = 开面板点一下）+ 分组单行模型清单（15 个全保留）；权限/运行中发送/统计同样入面板（统计摘要常驻菜单行）；投影/目录异步到位时菜单+打开中的面板一起刷新；面板内触摸不再触发下拽关抽屉。顺带：权限选项宿主只给原始值（danger-full-access 等），补了中文标签与说明（只读/工作区写入/完全访问） | ✅ |
| 62 | URL 还会吞 `"`：`href="http://x"` 这类行内代码里的闭引号经 esc 变成 `&quot;`，`&` 不在排除列表 → 整个实体被吞进 href（实测 `…sheet.html%22`）；且浅色主题里链接被刷成实心蓝底（好难看） | 重写为 linkifyRaw()：在**未转义**的原始文本上切分 URL、再分段转义，实体根本不会进入匹配（引号本就被字符集排除）；退化匹配（如 `http://.`）不再成链。样式：链接 = 蓝字 + 半透明下划线 + 无背景（用户指定），顺带修浅色主题把 `color:#2563eb` 误写成 `background` 的老 typo——该规则殃及链接/运行中徽章/工具图标/选中 chip（全部被实心蓝底覆盖），现已按「文字走 color、状态点走 background」拆分 | ✅ |
| 63 | 重试发送换新 rpcId：宿主按 requestId 幂等去重，新 id 等于放弃去重——首次已被受理只是回包迟到时，同一条指令会真的执行两遍 | sendPrompt 增加 reuseRpcId 参数，retrySend 沿用原 item.rpcId；实测拦截首次请求使其失败、点重试，两次请求 requestId 完全一致且消息最终送达一次 | ✅ |
| 64 | 搜索无结果复用「还没有会话」空态（会被读成『我的会话没了』）；冷启动数据未到也命中同一分支 | 空态按语境分岔：有搜索词→「没有标题或路径含 X 的会话」+ 范围说明 + 清除按钮；列表未落地→「正在加载会话…」；真没有→「还没有会话 点右下角＋新建」（原文案指称的按钮实际是＋图标，一并修正） | ✅ |
| 65 | 排队中的纯图片消息两头都不显示（chip 渲染跳过无文本项，乐观气泡又被排队逻辑撤下），直到被消费才「凭空出现」 | chip 跳过条件改为「无文本且无图片」；纯图片用「图片 × N」摘要；实测 vision 模型下排队截图即刻出现在 chip 条、对话流干净、消费后正常落流（glm-5.3 不收图片，属宿主模型能力，失败气泡+重试+toast 均正常） | ✅ |
| 66 | 浅色主题四个语义色沿用深色值（白底 2.1-3.5:1），「待办/等待审批」最该被看见的状态反而最看不清；--text-3 浅色值 2.85-3.09:1；.s-ws 白底白纱 | html.light 补语义色（--orange #9a4a06 / --green #15803d / --red #c1121f / --purple #6d28d9，实测白底 5.0-7.1:1）、--text-3 提到 #626b78（5.39:1）、.s-ws 补浅色底 | ✅ |
| 67 | --accent 同时当深底文字色和白字填充色：用户气泡/FAB/主按钮白字仅 3.68:1；「发送中」再压 opacity:.62 到 2.84:1 | 拆 --accent-fill(#2563eb，白字 5.17:1) 专供填充，7 处调用点换 token（深色下观感几乎无差别）；pending 气泡去掉 opacity、保留虚线描边表达状态 | ✅ |
| 68 | 重连静默清空待审批/提问（评审 P0-1）。实测修正其前提：当前宿主会把未决 waterfall 重放给新连接（同 eventId 回到新 clientId），直接清空+挂「已失效」反而说谎 | 清空旧条目后留 2.5s 重放窗口再记账：重放回来的卡片原样恢复、不打扰（实测 ✓）；真正没回来的才挂「N 项失效，在桌面端处理」常驻条（注入宿主不认识的孤儿审批实测 ✓，可关闭）；清空后当前会话立即重渲染，避免死卡上还留着可点的允许/拒绝 | ✅ |
| 69 | 「待办」筛选归零后退不出：chip 隐藏时 todoMode 置 false 但不重渲染，列表停留在筛选后 DOM，唯一解除入口已消失 | refreshBadges 归零分支补 renderList()+updateTabs()；经真实调用路径（路由往返）实测 todoMode 清除、79 卡恢复、空态消失 | ✅ |
| 70 | markdown 四级标题同字号（16px），Agent 的结构化回答被压平 | 三级字号（h1 19 / h2 17 / h3 15.5 / h4 14+次级色）并收紧层级边距；实测计算样式逐级递减 | ✅ |
| 71 | 表格降级成竖线等宽文本，390px 上列永远对不齐 | flushTable 输出真 <table>：--- 分隔行判定表头、数字列自动右对齐等宽、横向滚动容器、发丝分隔；实测 th 6px10px / td.num 右对齐 / overflow-x auto | ✅ |
| 72 | 注释承诺的斜体没实现，~~删除~~ 与 ![]() 原样漏出 | 补 em / del / 图片语法（图片渲染为 [图]链接，不残留感叹号）；乘号边界（5*3*2）不误伤 | ✅ |
| 73 | 触控热区大面积 <44px（最小 20px 的图片删除 ×） | 十二类小控件用透明 ::after 扩热区（视觉尺寸不变）：× 20→44、思考/复制 26-30→42-44、停止/分段 34→44、发送/＋/待办 38→44 | ✅ |
| 74 | 会话页断线只改副标题文案，无重连入口，与列表页口径不一 | 输入区上方断线条：「连接已断开 · 后台每 15 秒自动重试」+「立即重连」（与列表页共用 manualReconnect）；实测真实断 WS → 条出现 → 重连后消失 | ✅ |
| 75 | 浮层关着的还在 Tab 序里，开着的 aria-hidden=true 读屏反而读不到，无 Esc | ovSet() 统一管理：关闭时 visibility:hidden 移出 Tab 序 + aria-hidden 双向同步（六个浮层全接入）；Esc 关闭最上层（栈序跟踪）；实测 Enter 打开面板、Esc 逐层关闭 | ✅ |
| 76 | 五类高频控件（chips/ask-opt/sheet-row/act-row/pick-ws）键盘摸不到；分段控件 role=tab 无 aria-selected；任务条有 role 却只绑 onclick | btnize() 补 role+tabindex + 全局 Enter/Space 派发（自带处理的跳过防双发）；分段控件改 aria-pressed 并随选中同步；任务条补键盘；实测菜单行 focus+Enter 打开模型面板 | ✅ |
| 77 | 「运行中蓝」#6aa6ff 硬编码 21 处无 token，浅色补丁各写一条还漏 7 处；杂色 #8b5cf6/#ffd48a 无主 | --info token（深 #6aa6ff / 浅 #2563eb）全量替换，浅色 8 条 color 补丁删除自动适配；#8b5cf6→--purple、#ffd48a→--warn-bright 归位；实测浅色计算值 #2563eb | ✅ |
| 78 | 死代码与死样式：initLongPressCopy 全文件无调用、tabbar/running-bar/load-earlier/tk-dot 无对应 DOM | 全部删除（app.js -45 行、style.css -40 行），残留引用一并清理 | ✅ |
| 79 | 滑动动作与操作单用 emoji 当图标（✏️⚡🗑⑂📦），与线性 SVG 不同源，无 emoji 字体时变空框 | 新增 trash/fork/archive 三个同风格 SVG，滑动动作与全部操作单换 SVG；toast 文案里的装饰符号一并清除 | ✅ |
| 80 | 工具卡输出静默截断在 4000 字符，且复制只能手动框选 | 超限显示「已截断」提示 + 每张卡「复制」按钮取完整输出；实测 48/48 卡片带复制钮 | ✅ |
| 81 | 复制链路：execCommand 失败也提示「已复制 ✓」；审批 outcome 拼出「已decided-elsewhere」；错误 toast nowrap 截断；预设加载失败静默；无工作区留着必然报错的主按钮 | copyText 汇报真实成败（全部调用点按结果提示）；outcome 全量中文映射；toast 允许换行（pre-wrap+max-height）；预设失败给出交代；无工作区时禁用按钮并说明 | ✅ |
| 82 | 用户反馈「在底部时新内容不自动下拉，要手点 ↓」（diff 核对 v1.1.2→HEAD 滚动逻辑零改动，排除回归；判定为 iOS WebKit 在惯性滚动/键盘聚焦期间丢弃单次 scrollTop 赋值） | scrollBottom 改为双帧钉底：赋值后下一帧复查再补一次；用户上滑脱离阈值即放弃，不抢滚动权；模拟连续内容增长实测跟随间隙全 0 | ✅ |
| 83 | 独立 PWA 里排队消息卡在「发送中」（Safari 正常）。主嫌疑：iOS PWA 从快照恢复时不重新加载页面，桌面 App 可能仍在跑 v0.9.7 之前的旧 JS（当时正是无超时兜底的「发送中」bug）；次嫌疑：PWA cookie 与 Safari 不共享/已过期，WS 旧连接让界面看着活着但新请求全部 401 | rpc 401 → 置顶常驻「登录已过期」横幅（role=alert，带刷新按钮，键盘弹起也可见）——用过期 cookie 实测横幅出现；处置指引：从多任务彻底划掉 PWA 重开（强制加载新代码） | ✅ |
| 84 | 列表可被左右拖出横向橡皮筋、卡片被斜向滚动拖歪（「歪歪扭扭」），左滑操作因此难触发；对话与工作区两个页签都受影响 | 三层修复：① `#list-scroll`/新会话页 `touch-action:pan-y + overflow-x:hidden`——容器只允许纵向滚动；② `.swipe-wrap` 加 `touch-action:pan-y`——方向仲裁交给系统（纵向归浏览器、横向归 JS），斜向起步不再误判；③ initSwipe 方向锁收紧（12px 起判 + 1.5 倍横向优势），列表滚动时自动收起已展开的左滑操作（iOS Mail 同款）。实测：纯左滑出操作、斜向/纵向拖动歪卡=0、容器横移=0、scroll 收起操作 | ✅ |
| 85 | 工作区分组与桌面口径分叉：未挂载会话（cwd 恰好等于某工作区路径）被 cwd 兜底错误归组——桌面显示「未归类」，手机却归进该工作区（实测：滚动测试残留的 5 个「数到60」会话 cwd=个人小工具、注册表未收录，手机错误归入） | findWs 删除 cwd 兜底，严格跟随宿主注册表 sessionIds；未挂载会话进「未分类」组（原「其他」改名，对齐桌面叫法）。测试残留会话已归档 | ✅ |
| 86 | 键盘弹起把「整页」顶上去：iOS 开键盘会先把 layout 视口上推（visualViewport.offsetTop）去够底部输入框，而旧代码只把应用高度钉成 vv.height、不管 offsetTop——应用与可视区域错位，表现就是全屏空白只剩键盘 | 应用改为精确覆盖可视视口：`top = vv.offsetTop`、`height = vv.height`——导航栏钉在屏幕顶、对话区压缩、输入框正好落在键盘上沿（「只顶一部分」）。模拟钉住态几何实测：app 底边 = composer 底边（gap=0）、navbar 可见、复原干净。iOS 真机行为需你在设备上确认 | ✅ |
| 87 | 下拉刷新与左滑互抢：PTR 在 touchstart（列表顶部）就武装，左滑卡片时向下漂移 >12px 就显示刷新指示、>72px 甚至真触发刷新——「一边滑一边晃」 | PTR 加方向锁：touchstart 不再武装，仅在「列表在顶部 + 明显下拉（纵向优势 1.5 倍）+ 无卡片处于左滑拖动态」三条件齐备时进入；touchcancel 一并复位。实测：顶部下拉中途显示指示/松手触发刷新、非顶部不武装、左滑+下漂不武装 | ✅ |
| 88 | 在底部时被「图片撑开」顶上去且再也回不来：① 钉底决策是每次渲染现量位置——图片解码后高度撑开把你顶离底部的一瞬间 nearBottom 变 false，此后所有渲染都当你「主动上滑」，永不跟随；② .msg-img 无尺寸占位，每次重建都是 0 高→撑开；③ 转正时乐观图片（带 previewUrl+尺寸）被事件折叠块（只剩 attachmentId）替换，又走回 80px 占位老路 | 系统性修复：a) 钉底改为跟随「用户意图」状态机——被顶开（无滚动事件）不丢跟随、渲染时自动回底，真实上滑（滚动事件 gap>120）才取消，回到底恢复；b) 压缩管线记录图片宽高，渲染给 aspect-ratio 占位（首帧即正确高度）；c) attachment 首次加载后缓存宽高比，重建零跳变；d) 转正优先保留乐观图片；e) 图片 load 事件在跟随态即时补钉。实测：注入 30 条内容后被顶开 420px（无滚动事件）→ follow 保持 → renderChat 自动回底 gap=0；真实上滑 420px → follow=false → 不抢滚动 | ✅ |
| 89 | 新功能：问过的问题导航（A1 方案）——⋯ 菜单加「问过的问题 · N」行，二级面板按时间倒序列出本对话用户消息，点击 → 关面板并居中定位 + 高亮闪烁；不在当前窗口时自动向前翻页寻找（≤12 页）。跳转即置 follow=false + 瞬时定位，修「运行中的会话跳转被 follow 拽回底部、smooth 动画被重建中断」（实测偏离中心 -6px、6 秒后仍停原位） | ✅ |
| 90 | 新功能：助手消息分叉（B1 方案，桌面端轮尾 branch 的忠实移植）——折叠事件记录 seq/turn，meta 行加 ⑂ 图标（仅已完成轮次、非子代理可见，运行中的当前轮自动隐藏）；轻确认后 session/fork（atSeq=该条事件 seq，服务端切到该轮 turn/end）→ increasedForkTitle 移植（标题 (1)/(n+1) 后缀）→ 直接打开子会话。实测：21 条助手消息 19 条有图标（2 条运行中轮次正确隐藏）、子会话「DSH mobile (1)」创建并打开、历史正好切在分叉点、fork-unavailable 有人话提示 | ✅ |
| 91 | 问过的问题只显示当前窗口（6 条），且只有时分——跨天的问题分不清 | collectAllQuestions()：向更早翻页（session/page 200 条/页，≤30 页）折叠进临时对象不动聊天区，全量按时间排序；时间直接用 fmtTime（自带 今天/昨天/M月D日/年份 分层）；jumpToItem 改按键匹配（seq 优先，time/文本兜底），翻页上限提到 50 页。实测本会话：窗口 6 → 全量 74 条，最旧 8月30日 23:58（会话第一条），点击后翻 2041 条全量载入并定位高亮 ✅ | ✅ |
| 92 | 新功能：任意内容引用（方案 A + 用户增强：每条内容下方常驻 ❝ 图标，点即引用，不弹操作条） | 用户消息/助手回答/工具卡（含命令+输出，超长 1500 字截断）/系统消息全部带 ❝ 引用图标；点按即成 chip（输入框上方横滑条，类型图标+摘要+注解橙点+✕，上限 6 条）；点 chip 开注解面板（全文+注解，可连续编辑/删除）；发送时编排成「> [来源] 引用 /【注】注解 / 正文」纯文本（协议只有 text，所见即模型所得）；气泡按分层块渲染（竖线引用块+橙色注解+正文），回读解析让重进会话后仍保持分层。实测：引用户+工具两条→注解→发送→宿主侧原文含 `> [用户]`/`【注】`/`> [工具·bash]`/正文 ✅，重进渲染 qblk×2+qnote+body ✅ | ✅ |
| 93 | 用户反馈三处：引用图标丑（自绘制 stroke 路径太复杂不好看）；⋯ 菜单「问过的问题」计数只显示当前窗口数（6 条）；注解输入超 3 行溢出框外 | ① 图标换 Material Design 的 format_quote（`fill=currentColor` 干净双引号，和 SF Symbols 风格协调）；② 进会话即后台算全量计数缓存到 `s._qTotal`（面板打开也顺手缓存），菜单显示缓存值，首次算不完显示 …，算完就刷新；③ `.note-in` 加 `max-height:110px; overflow-y:auto`。实测：菜单 76 条（全量）✅、图标 fill 模式 ✅、注解 7 行限高 110px 滚动 ✅ | ✅ |
| 94 | 用户反馈两处：① 发送包含引用的消息后气泡里全文显示（工具输出 1500 字 → 气泡占半屏）；② 引用注解面板第一次弹出时看不到界面只能盲打——`.sheet-overlay` 是 `position:fixed` 定位到全屏，键盘弹起后 sheet 沉到键盘后面 | ① `.qblk` 加 `-webkit-line-clamp:2`（气泡内每条引用截到 2 行，全文在注解编辑器里看）；② `.sheet-overlay` 从 `position:fixed` 改为 `position:absolute`——overlay 是 `#app` 的子元素，`#app` 是 `position:fixed` 且键盘弹起时 `height=vv.height`，absolute 子元素自动被约束在可视视口内。实测：模拟键盘弹起（app 高度缩到 440px）后 sheet bottom=520=app 底边（过渡动画结束后）✅；引用块 2 行截断 h=39px ✅ | ✅ |
| 95 | 新功能（P1+P4）：轮级统计——① 助手消息 meta 行加紧凑 pill `⚡ 198.7s · 46 t/s · 147万`（轮时长·速度·总 tok），点开明细面板（消耗：输入/缓存命中%/读/输出/思考/总上下文 + 耗时：总时长/速度 + 模型路由）；② 统计面板补「平均 TTFT」「平均速度」两行（sessionStats 现成字段，桌面统计对话框同款公式） | 数据全部来自事件流（assistant/message.usage + turn/start→end 时间差），零新接口。实测：本会话最近 5 轮都有 pill（198.7s·46t/s·147万 等），明细面板 7 格+3 组正常，统计面板 10 格含平均 TTFT 8.7s / 平均速度 61 tok/s | ✅ |
| 96 | 用户反馈：编辑排队内容时，文本一长就超出编辑框，框外那段根本选不中、改不了。根因：`.q-edit-box` 只有 `min-height` 没上限，而父级 `.sheet` 是 `max-height:72%` + `overflow:hidden`——编辑框一路长高、单子照裁，超出部分既不滚动也选不到（实测旧结构：单子内容 1109px / 可视 742px，被裁 367px）。另外正文用 `textContent` 直写、编辑框没 `white-space:pre-wrap`，排队文本里的换行在框里不显示，经 `textContent` 读回也丢行 | ① 操作单正文抽成 `.sheet-scroll.q-body`（flex:1 + min-height:0）自己滚，编辑框不再设高度上限——整段文字都在可滚动内容里，长按选取/拖手柄到边缘时由该滚动区自动滚；②「保存修改」移出滚动区做单子底栏（常驻可见，不用滚到底去找）；③ 编辑态给单子加 `.editing`（max-height 72%→88%，键盘弹起后 72% 只剩一小条），并收起已在编辑的那行入口；④ `.q-edit-box` 补 `white-space:pre-wrap` + `overflow-wrap:break-word` + `-webkit-user-modify:read-write-plaintext-only`；⑤ 读回改走 `editableText()`（innerText 优先，`<br>`/`<div>` 换行还原成 `\n`）；⑥ 关单子时 blur 编辑框（焦点留在已关闭的框上，iOS 键盘会挂在屏幕上）；会话「重命名」框同一处结构一并修（共用 `.q-edit-box`/`.q-body`）。实测（真实页面 + 宿主队列原文交叉核对）：677 字排队文本在编辑态单子 742px 不再裁切（旧结构 A/B 复现裁切 367px）、正文区可滚、末行可见鼠标命中偏移 = 文本长度（尾巴确实在可视区）、Range 全选 677/677 字、全选后键盘输入整段替换 → 宿主队列原文逐字一致、滚到底后只替换末尾 13 字 → 宿主侧 15 行换行全保真且第一项未被误改、保存按钮在 844/420 两种视口高度都完整可见、关单子后焦点回 BODY。iOS 真机的手柄拖选自动滚动手感仍需你在设备上确认 | ✅ |
| 97 | 用户反馈：点 ❝ 引用之后底部只多了一个 chip，引用注解面板压根没打开，更谈不上「直接写注解 + 弹键盘」。真实页面复现：点 ❝ → `#quote-ov` 仍 hidden、焦点在 BODY；只有点 chip 才开面板，而 `note.focus()` 被放在 320ms 的 `setTimeout` 里——已脱离用户手势，iOS 判定非用户触发、不弹键盘（v1.4.6 那次「延迟聚焦」正是这个坑）。顺带查出回读 bug：多行注解 `【注】a\nb` 只解析第一行，第二行连同后面的引用块一起漏进正文，重进会话气泡分层错乱 | ① ❝ 一键引用：加 chip 后同步 `openQuoteSheet`（点引用就是想写注解，不必再点一次 chip）；② 新增 `focusNote()`：在手势内同步 `focus({preventScroll:true})` + 光标落末尾，未生效才 rAF 兜底；③ `parseQuotedMessage` 的【注】支持多行（读到空行或下一条 `> [` 为止）；④ `.note-in` 补 `white-space:pre-wrap` + `overflow-wrap:break-word`，保存改走 `editableText()`。实测：点 ❝ 面板 visible、注解框在手势内已聚焦（`userActivation.isActive=true`、`activeElement=quote-note`），不做任何额外点击直接打字落进注解框；二次点 chip 光标在末尾可继续追加；chip ✕ 与面板「删除引用」正常；发送后宿主侧原文为 `> [助手] …【注】注解第一行\n注解第二行 … 正文`；重进会话气泡＝3 引用块 + 2 注解（多行保真）+ 正文干净、无 `> [` 泄漏。iOS 真机是否弹键盘仍需你在设备上确认 | ✅ |
| 98 | 用户指出 P1/P4 没做完。复查原型 `prototype/stats.html` 后确认 P1 确实只做了一半：轮尾明细面板缺「缓存写入 / TTFT（首 token）/ 解码时长」三格；pill 的「速度」用的是 `输出 ÷ 轮总时长`，而原型与桌面（`deriveTurnMetrics.tokensPerSecond`）用的都是**解码速度** = Σ输出 ÷ Σ解码时长；更严重的是多步轮的消耗按「渲染成气泡的助手消息」汇总——纯思考步/工具步不渲染成气泡，它们的 usage 被整段丢掉（实测带工具的一轮：页面 输出 2 / 缓存读取 1.3万，宿主真值 输出 817 / 缓存读取 2.6万，少算一个数量级）。P4 两行公式本身没问题，但统计面板把「LLM 时间 7.3 秒」显示成「0 分钟」 | ① foldEvent 新增 `step/start`、`assistant/attempt` 两个事件的折叠（宿主 session-stats 同款状态机：步起点时间 + 流内首 token 时间），`assistant/message` 结算该步读数（llmMs/TTFT/解码时长/输出 token），`turn/end` 汇总：TTFT 取本轮最低步、解码时长与输出 token 只累加同时有数据的步、速度＝解码速度；② usage 改为**事件驱动按步累计**（`s._turnUsage`，纯思考步/工具步的 token 不再丢），与桌面 `aggregateAttempts` 求和口径一致；③ 明细面板补三格，耗时组＝总时长/TTFT/解码时长/速度（桌面 TurnTimePanel 同款字段）；④ `fmtDur` 对齐桌面 `formatDuration`：不足 60 秒给秒、不足 1 小时给「N 分 M 秒」。实测（宿主侧用同一套公式独立实现，从 `session/page` 原始事件算真值逐格比对）：三轮（含一条两条 bash 的多步轮）明细 9 格**全部逐字一致**——多步轮 输入 375 / 缓存读取 2.6万 / 输出 817 / 缓存命中 98.6% / TTFT 0.8 秒 / 解码 3.9 秒 / 速度 211 tok/s（＝817÷3.9s，自洽）；pill 为 `⚡ 5.5s · 211 t/s · 2.7万`；P4 面板 10 格，平均 TTFT 0.8s、平均速度 210 tok/s 与 `sessionStats` 投影公式逐字一致，LLM 时间 7.3 秒 / 工具时间 0.1 秒 | ✅ |
| 60 | 空闲会话长按发送：toast 说「本次将插话发送 ⚡」但实际以 queue 发出（空闲时 queue/steer 无差别）——承诺与行为不符 | 长按定时器回调检查 `s.running`：空闲按普通发送处理、不弹模式 toast；运行中行为不变（反向 + toast + 震动）。端到端实证见 `verify/REPORT.md`（T2b/T4b） | ✅ |
| 61 | 空输入长按发送：震动 + toast「本次将插话发送」后 `doSend` 空输入静默早退——被提示却什么都没发 | 定时器回调先判空（文本 + pendingImages），为空直接取消：不震动、不提示、不发。实证见 `verify/REPORT.md`（T7/T7b） | ✅ |
| 62 | 打开/关闭「排队操作单」各抛一次 `Cannot read properties of null (reading 'classList')`：`openQSheet`/`closeQSheet` 取 `$('#q-sheet')`，但 buildShell 静态结构里该节点只有 class 没有 id（会话菜单的 `sess-sheet` 是有 id 的，此处漏加）。抽屉滑入靠 `.sheet-overlay.open .sheet` 后代选择器，视觉碰巧正常，异常一直静默 | 节点补 `id="q-sheet"`。鼠标路径验证（`verify/mouse-click.mjs` M13/M15）首次捕获并实证，修复后全程零 pageerror | ✅ |

## 仍需协议/服务端配合（本轮未动）

- 列表接口携带末条消息预览与未读计数（冷启动列表只有 cwd）。
- 会话管理（归档/重命名/删除）下发到移动端，配合长按菜单。
- 排队消息的取消/编辑。
- Web Push：后台时审批/提问的系统级通知。
- Service Worker 离线壳未做：主要访问路径是 `http://<tailscale>:<port>`（非安全上下文，SW 不可注册），需先有 https 才有意义。
